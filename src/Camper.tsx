export default class Camper {
    name: string;
    recent: number;
    sum: number;

    constructor(name: string, recent: number, sum: number) {
        this.name = name;
        this.recent = recent;
        this.sum = sum;
    }
}